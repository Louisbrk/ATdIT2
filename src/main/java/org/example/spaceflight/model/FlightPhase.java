package org.example.spaceflight.model;

/** The five phases of the space tourism flight from launch to landing. */
public enum FlightPhase {
    PRE_FLIGHT,
    ASCENT,
    ORBIT,
    DESCENT,
    LANDED
}
