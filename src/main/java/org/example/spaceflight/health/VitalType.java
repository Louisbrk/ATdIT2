package org.example.spaceflight.health;

/** Identifies each monitored vital sign — used as key in evaluation results. */
public enum VitalType {
    BPM,
    SPO2,
    SYSTOLIC_BP,
    DIASTOLIC_BP,
    RESP_RATE
}
